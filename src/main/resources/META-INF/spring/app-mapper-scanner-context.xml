<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="ds" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="oracle.jdbc.OracleDriver" />
		<property name="url" value="jdbc:oracle:thin:@localhost:1521:xe" />
		<property name="username" value="hr" />
		<property name="password" value="zxcv1234" />
	</bean>
	
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="ds" />
		<property name="configLocation" value="classpath:/META-INF/mybatis/mybatis-config.xml" />
	</bean>
	
	<!--  
		MapperScannerConfigurer은
		지정된 패키지에 정의된 모든 Mapper 인터페이스를 읽어서 
		Mapper 인터페이스를 구현한 Mapper 인스턴스(구현객체)를 생성하고, 
		생성된 구현객체를 모두 스프링의 bean으로 등록한다.
	-->
	
	<!-- 지정된 패키지 경로에 있는 모든 mapper 인터페이스를 읽어서 구현객체를 만든 후, 스프링의 빈으로 만들어줌 -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage"  value="com.sample.demo1.mappers" />
		<!-- sqlSessionFactoryBeanName -> 빈의 이름을 알려달라는 요청이므로 value에 이름만 전달 -->
		<property name="sqlSessionFactoryBeanName"  value="sqlSessionFactory" />
	</bean>

</beans>
